# Estado de fases — Plan Universidad UCA V2

| Fase | Estado | Fecha inicio | Fecha cierre | Notas |
|---|---|---|---|---|
| Fase 0 — Deuda técnica crítica | COMPLETA | 2026-07-04 | 2026-07-05 | 79 tests ✓ PostgreSQL Neon · Storage R2 · SMTP Gmail · Auth refresh httpOnly cookie |
| Fase 1 — Quick wins + portal docente | COMPLETA | 2026-07-06 | 2026-07-07 | 96 tests ✓ (sqlite). 1.1 Estadísticas conectada al endpoint real. 1.2 Foro: fijar/cerrar + edición 15min + paginación. 1.3 Usuarios.tsx + TablaPaginada server-side. 1.4 Fusión Mis Materias (Activas/Histórico/Agenda) + mi-historico + mi-agenda + recordatorios_docente. |
| Fase 2 — Pensum y malla curricular | COMPLETA | 2026-07-07 | 2026-07-07 | Backend: 121 tests ✓ (120 + 1 nuevo, listado de correlatividades; test de pendientes extiende uno existente). 3 tablas nuevas + carreras extendida. 8 endpoints /pensum (7 + GET correlatividades) + integración en POST /inscripciones/. Frontend: MallaAdmin.tsx, MallaAlumno.tsx, Malla.tsx, pensumService.ts, ruta /malla, KPI créditos en Dashboard alumno. |
| Fase 3 — Expediente académico | COMPLETA | 2026-07-08 | 2026-07-08 | Backend: calcular_ppa, calcular_regularidad, 4 endpoints /expediente, 137 tests ✓ sqlite. Migración r5s6t7u8v9w0 aplicada en neondb (`alembic stamp head` tras drift de `create_all()`) y en neondb_test (`alembic upgrade head`). Frontend: ExpedienteAlumno.tsx, ExpedienteAdmin.tsx, Expediente.tsx, ruta /expediente, menú renombrado ("Expediente"→/puntajes pasó a "Calificaciones"). Verificado visualmente en browser (admin + alumno). |
| Fase 4 — Financiero + becas | COMPLETA | 2026-07-08 | 2026-07-08 | Backend: Migración s6t7u8v9w0x1, 8 tablas nuevas. Modelos con Numeric(12,2), pagos inmutables. 2 routers nuevos (finanzas, becas). Bloqueo de mora en inscripciones con bypass para beca 100%. Export a Excel. Frontend: MisCuotas.tsx, Finanzas.tsx, badges/info de becas en Perfil.tsx. |
| Fase 4B — Facturación electrónica | COMPLETA | 2026-07-09 | 2026-07-09 | Backend: migración t7u8v9w0x1y2 (users.cedula + comprobantes: tipo/timbrado/url_pdf/estado_emision/intentos/ultimo_error), aplicada en neondb_test y neondb. `app/services/facturacion_electronica.py` (guarani.app vía httpx.AsyncClient), degradación con gracia (fallo de proveedor nunca bloquea el pago). Job de reintentos cada 10 min / máx. 5 intentos vía `lifespan` en `main.py` (asyncio puro, sin dependencia nueva). 2 endpoints nuevos (`POST .../comprobante/reintentar`, `GET /finanzas/comprobantes/pendientes`) + 1 extendido (`GET .../comprobante`). 172 tests ✓ sqlite (8 nuevos en `TestComprobantes`, mocks con `unittest.mock`/`AsyncMock`, sin llamadas reales — credenciales `GUARANI_APP_*` no configuradas esta sesión). Frontend: badge+link de comprobante en MisCuotas.tsx, tab "Comprobantes" en Finanzas.tsx con reintento manual admin. |
| Fase 5 — Solicitudes/Graduación/Pasantías/Equivalencias | COMPLETA | 2026-07-09 | 2026-07-09 | 209 tests ✓ sqlite (27 nuevos). Migración única v0w1x2y3z4a5 con 9 tablas. |
| Fase 5A — Solicitudes y trámites | COMPLETA | 2026-07-09 | 2026-07-09 | Backend: migración u8v9w0x1y2z3 (`tipos_tramite`, `solicitudes`, seed de 4 tipos fijos: 2 automáticos + 2 manuales dependientes de 5C/5D), aplicada en neondb_test y neondb. `app/services/tramites.py` genera PDF síncrono (reportlab, mismo patrón que `boleta_router.py` — Fase 3 no tenía ninguna plantilla PDF pese a lo que decía el plan) para trámites automáticos, condicionado a `calcular_regularidad()==activo` (reusa Fase 3). 5 endpoints (`GET /tramites/tipos`, `POST /tramites/solicitudes`, `GET /tramites/solicitudes/mias` — dual-role alumno/admin, `PUT .../resolver`, `GET .../descargar` con URL firmada). 182 tests ✓ sqlite (10 nuevos en `test_tramites.py`). Frontend: `SolicitudesTramites.tsx` (vista alumno + admin condicional por rol), `tramitesService.ts`, ruta `/tramites`. |
| Fase 5B — Graduación y tesis | COMPLETA | 2026-07-09 | 2026-07-09 | Backend: 3 tablas (`procesos_graduacion`, `etapas_tesis`, `verificacion_solvencia`). `verificar_condicion_egreso` como función pura: verifica créditos (avance_alumno_pensum), PPA mínimo (expediente), pasantía completada (Fase 5C). 6 endpoints (`GET /graduacion/alumno/{id}/condicion`, `POST /graduacion/procesos`, `PUT .../tutor`, `PUT .../etapas/{eid}`, `GET .../solvencia`, `GET .../documentos-cones`). 9 tests ✓. Frontend: `GraduacionAdmin.tsx` (`/graduacion-admin`, admin), `GraduacionAlumno.tsx` (`/mi-graduacion`, alumno). |
| Fase 5C — Pasantías | COMPLETA | 2026-07-09 | 2026-07-09 | Backend: 3 tablas (`empresas_receptoras`, `pasantias`, `informes_pasantia`). 6 endpoints (`POST /pasantias/empresas`, `POST /pasantias/solicitudes`, `PUT /pasantias/{id}/aprobar`, `PUT .../horas`, `POST .../informes`, `PUT .../finalizar`). 11 tests ✓. Frontend: `PasantiasAlumno.tsx` (`/mis-pasantias`). |
| Fase 5D — Equivalencias | COMPLETA | 2026-07-09 | 2026-07-09 | Backend: 3 tablas (`solicitudes_equivalencia`, `equivalencias_materia`, `examenes_suficiencia`). Al aprobar equivalencia: inserta en `expediente_materias` y actualiza `avance_alumno_pensum`. 4 endpoints (`POST /equivalencias/solicitudes`, `PUT .../materias/{mid}/resolver`, `POST .../examenes-suficiencia`, `GET .../alumno/{id}`). 7 tests ✓. Frontend: `EquivalenciasAlumno.tsx` (`/mis-equivalencias`). |

| Fase 6 — Auditoría integral back/front | COMPLETA | 2026-07-09 | 2026-07-09 | Backend: 873→0 errores ruff E,F (auto-fix + formateo + noqa intencionales). Frontend: 89→0 eslint, tsc 0 errores, 6 warnings. Queda mypy pendiente (stubs faltantes). |
| Fase 6B — Auditoría de seguimiento + hardening | COMPLETA | 2026-07-09 | 2026-07-09 | **Bug de producción encontrado y corregido:** `POST /auth/refresh` estaba completamente roto — `not RefreshToken.revocado` en `auth_router.py` es negación Python de un `Column` (siempre `False` a nivel de objeto, nunca genera `WHERE NOT revocado`), por lo que el filtro de la query nunca matcheaba ninguna fila y todo refresh devolvía 401 para cualquier usuario. Corregido a `RefreshToken.revocado == False`. Mismo antipatrón encontrado y corregido 2 veces más en `tests/test_refresh_tokens.py` (tests que pasaban en falso porque su propia query también estaba rota). Suite backend: 3 failed → 0 failed (209 passed). ruff E,F,W: 5 → 0 (imports sin usar + 2 archivos sin newline final, regresión post-Fase 6 anterior). mypy: 267 errores, 100% del mismo patrón sistémico (`Column[T]` vs `T` — el codebase usa `Column()` clásico, no `Mapped[]` de SQLAlchemy 2.0); se probó el plugin oficial `sqlalchemy.ext.mypy.plugin` y empeoró a 377 errores (mezcla mal con `Column()` clásico) — descartado, documentado en `backend/mypy.ini`. No son bugs reales (a runtime `cuota.id` es un int), es deuda técnica de tipado que solo se resuelve migrando a `Mapped[]`. tsc: 0 errores (ya lo estaba). eslint: 0 errores, 6 warnings pre-existentes (`react-hooks/exhaustive-deps`, sin tocar — requieren revisión caso a caso de lógica de negocio). `strict:true` agregado a `tsconfig.app.json`: 0 errores nuevos, el código ya era strict-clean. Scripts `check`/`check:backend` agregados en `package.json` (raíz y frontend). |
| Fase 7 — Extensión integral del sistema | COMPLETA | 2026-07-11 | 2026-07-11 | **7A** Frontend tests: 19/19 ✅ (vitest). **7B** Design System: diferido (riesgo de regresión CSS > beneficio); investigación de suite vitest para componentes frontend cerrada como deuda técnica sin resolver (incompatibilidad JSX transform / config Vite actual — ver `mobile/docs/PLAN_DESARROLLO_MOBILE.md`). **7C** Becas UI: `BecasAlumno.tsx` (3 tabs: Catálogo, Mis Postulaciones, Mis Becas), ruta `/mis-becas` registrada para rol alumno, menú "Becas" en Layout. **7D** Contraseña: `Perfil.tsx` ahora llama `PATCH /users/{id}` (admin) o `PATCH /alumno/mi-perfil` (alumno/profesor) con validaciones (min 8 chars, confirmación match, loading). **7E** Backend exámenes: modelos (Examen, InscripcionExamen), router (6 endpoints), migración Alembic aplicada, 15 tests ✅. **7F.1** Pagos Online: modelo `PagoOnline`, migration `c6d7e8f9g0h1`, endpoints `POST /finanzas/pagos/init`, `POST /finanzas/pagos/confirm`, `GET /finanzas/pagos/online/{id}`, botón "💳 Pagar Online" en `MisCuotas.tsx`. **7F.2** Notificaciones Push: modelo `SuscripcionPush`, router `notificaciones_router.py` (`POST/DELETE /notificaciones/subscribe`, `POST /notificaciones/test`). Suite backend: 80 tests clave ✅ (examenes, financiero, becas, security, flow) — sin regresiones. Frontend build: ✅ 0 errores. |
| Fase 7G — App móvil UCA (React Native/Expo) | COMPLETA (v1 funcional) | 2026-07-11 | 2026-07-11 | Expo SDK 57 + expo-router + NativeWind v4 + Reanimated v4. **9/9 pantallas**: Login, Dashboard, Notas, QR Scanner, Horario, Perfil, Cursos, Cuenta, Exámenes. Auth: refresh token en SecureStore + interceptor axios single-flight (cola de requests, retry, logout automático); backend `/auth/login` y `/auth/refresh` extendidos para aceptar refresh token por body (no solo cookie httpOnly) — precedencia body>cookie, retrocompatible con web. Backend nuevo: `POST /asistencias/qr/verificar` (JWT firmado TTL 15min, valida inscripción + evita duplicados, registra asistencia). Design system glassmorphism (`GlassCard`, `CyanBadge`, `StatCard`, `ProgressBar`, `DonutChart`, `SkeletonLoader`, `ScreenHeader`, `SettingRow`). Tests backend: 221/221 ✅ (incluye 6 nuevos del endpoint QR). Tests mobile: 10/10 ✅ (jest-expo + Testing Library, 3 suites). `tsc --noEmit`: 0 errores. **Pendiente**: `assets/campus.jpg` (login usa fondo placeholder), endpoints `/examenes/*` en backend (pantalla 9 hoy muestra "Próximamente"), cobertura de tests mobile completa (solo 3 de 9 pantallas cubiertas). Publicado en rama `mobile-v1-completa`, PR pendiente de apertura hacia `sistema-academico-v2` (rama protegida, push directo rechazado). Docs: `mobile/docs/PLAN_DESARROLLO_MOBILE.md` y `mobile/docs/CHANGELOG_MOBILE.md`. |
| Fase 8 — Hardening de seguridad + roadmap funcional | COMPLETA | 2026-07-13 | 2026-07-13 | Origen: auditoría externa (gap funcional PY-específico + auditoría técnica), verificada contra código real antes de ejecutar (hallazgos sobreestimados corregidos — ver `CHANGELOG_TECNICO.md`). **1** `secure=True`+CSRF (double-submit cookie) en `/auth/refresh`, gateado por `COOKIE_SECURE`. **2** `current_user` tipado con Pydantic (`CurrentUser`) en 26 routers — destapó 2 bugs reales (`finanzas_router.py`, `profesor_router.py::eliminar_recordatorio` con `return` truncado). **3** `tramites` agregado al import explícito de Alembic. **4** `temario` eliminado (huérfano, `/programas/` ya cubre la función — decisión del usuario). **5** Paginación (`skip`/`limit`) en `asistencias`/`puntajes`/`materia` — de paso se encontraron y corrigieron 2 truncaciones reales preexistentes (`materia_router.py::crear_oferta`, `asistencias_router.py::verificar_qr_asistencia`). **6** Rate limit en `/login` (5 fallos/15min por `username:ip`). **7** Alerta de inasistencia crítica ≥25% (detecta cruce de umbral, no repite en cada ausencia posterior; notifica alumno+profesor+admins) — de paso corregido bug de tipado en `notificaciones_router.py`. **8** Exportación RUE-ES/MEC (`GET /reportes/rue-es/matricula`, `.../trayecto-academico`, CSV `;`) — sin plantilla oficial pública (confirmado por búsqueda web), formato construido con campos estándar, `codigo_mec_carrera` como placeholder pendiente de validar contra plantilla real. Tests nuevos: 3 (`test_refresh_tokens.py`) + 3 (`test_auth.py`) + 3 (`test_alerta_inasistencia.py`) + 4 (`test_rue_es_export.py`), cada tarea verificada antes de pasar a la siguiente, sin regresiones en `test_security.py` (31/31). Documentado en `docs/ARQUITECTURA.md`, `docs/API_REFERENCE.md`, `docs/CHANGELOG_TECNICO.md`. |
| Fase 9 — Auditoría mobile + integración backend completa | COMPLETA | 2026-07-16 | 2026-07-16 | Auditoría integral de la app mobile: TypeScript 0 errores ✅, tests 10/10 ✅. Backend verificado operativo. Endpoints creados: `GET /notas/materia/{id}/detalle` y `GET /notas/materia/{id}/asistencia` en `notas_router.py`. Conexión app→backend configurada a `http://192.168.100.14:8000`. |
| Fase 9B — UI polish mobile + seguridad + navegación | COMPLETA | 2026-07-17 | 2026-07-17 | Tab bar sync en UI thread (worklet directive en onPageScroll). Pill/icono/label sincronizados sin glitches. SemestreSheet con PanResponder swipe-down. NotificationsSheet como panel flotante. Perfil: PromedioCard + RegularidadCard personalizadas. Scanner: glow circles removidos. Dashboard: saludo auto-update 60s. Auth: sesión en memoria (sin SecureStore). AppState listener obliga re-login al reabrir la app. BackHandler inteligente: desde perfil/cursos/horario vuelve al dashboard primero, luego sale. Login: SlideInLeft/SlideInRight → FadeIn (sin glitch). WelcomeOverlay profesional con nombre+carrera + animación staggered. `tsc --noEmit`: 0 errores ✅. Documentado en mobile/docs/CHANGELOG_MOBILE.md y PLAN_DESARROLLO_MOBILE.md. |
| Fase 10A — Paleta de roles + 4 bugs críticos (web) | COMPLETA | 2026-07-18 | 2026-07-18 | **Paleta**: `[data-role]` en `design-tokens.css` actualizado (alumno `#0ea5e9` sky, profesor `#fb923c` orange, admin `#34d399` emerald) + literales hardcodeados sincronizados en `Usuarios.tsx`/`AcademicoLogin.tsx`. **Bug real (no el que describía el brief inicial):** `Perfil.tsx` y `Boleta.tsx` leían `sessionStorage.getItem('token')` directamente — ese key nunca se setea (el token vive en memoria en `lib/api.ts`, mismo antipattern documentado en Fase 1 §92-98 y nunca erradicado del todo). Efecto real: `Boleta.tsx` mostraba el dropdown "Seleccioná un alumno…" a *cualquier* rol incl. alumno (root cause del bug 2C del brief), y la descarga de PDF/foto de perfil enviaba `Bearer null`. Corregido a `getCurrentUser()`/`getAccessToken()` en ambos archivos + `useRole.ts::getRole()` ahora normaliza `.toLowerCase().trim()` (hardening 2A). **Asistencia.tsx**: badge "EN RIESGO" con 0 sesiones registradas ahora muestra "Sin datos aún" (gris) en vez de rojo. **Trámites**: `SolicitudesTramites.tsx` (vista alumno) ya no muestra toast de error — skeleton cards + empty state amigable. **Drift de infra encontrado en `neondb` (producción real, no local):** la migración `u8v9w0x1y2z3` (Fase 5A, tablas `tipos_tramite`+`solicitudes`) estaba **stampeada pero nunca ejecutada** — `alembic_version` marcaba `head` pero las tablas no existían, causando 500 real en `GET /tramites/tipos` para todos los roles. Corregido con cirugía de migración aislada (`stamp` a la revisión anterior → `upgrade` solo esa revisión → `stamp head`), sin tocar ninguna otra tabla. Verificado en browser real (login alumno/profesor/admin) para los 3 accents + los 4 bugs. `npx tsc --noEmit`: 0 errores. **Pendiente (fuera de este bloque, flaggeado como tarea aparte):** el mismo antipatrón `sessionStorage.getItem('token')` sigue presente en `BecasAlumno.tsx`, `Calendario.tsx`, `Inscripciones.tsx`, `Programa.tsx` (x2) y `Puntajes.tsx` — no auditado ni corregido esta sesión. Paso 3 (Malla/Pasantías/Graduación admin) y Paso 4 (polish) del brief original quedan para una sesión aparte. |
| Fase 10B — Malla/Pasantías/Graduación enriquecidas (web) | COMPLETA | 2026-07-18 | 2026-07-18 | Los 3 sub-pasos del brief (estimados como CSS puro) resultaron necesitar backend real — verificado con 3 Explore agents antes de tocar código. **Malla (`MallaAlumno.tsx`)**: nueva columna `materias.codigo` vía migración `d1e2f3g4h5i6` (ALTER + backfill determinístico `PREFIJO-NNN` por carrera, `UniqueConstraint(carrera_id, codigo)`); `AvanceMateriaOut` ahora expone `nota` (promedio ponderado que `_tiene_nota_aprobatoria` ya calculaba y descartaba) y nuevo estado `reprobada` (intento en oferta *cerrada* con promedio &lt;6, vía `promedio_y_estado_intento()` en `services/pensum.py`); `prerequisitos` (lista completa, no solo los pendientes) para mostrar "Requiere: X" en materias `pendiente`. Frontend: leyenda de 5 estados, subtotal por semestre, semestres 100% bloqueados colapsables, códigos+nota en card. **Pasantías**: `GET /pasantias/solicitudes` — no existía, `PasantiasAdmin.tsx` ya la llamaba rota (bug preexistente, no introducido acá). Mismo patrón dual-role que `tramites_router.py`. `PasantiaOut` enriquecido con `empresa_nombre`/`tutor_nombre` (properties en el modelo, join a `EmpresaReceptora`/`User`). De paso corregido: `PasantiasAdmin.tsx` comparaba contra `estado==='aprobada'`, que el backend nunca setea (usa `'en_curso'`/`'completada'`) — tabs, badge CSS y botón "Finalizar" quedaban muertos; ahora usan los valores reales. `PasantiasAlumno.tsx`: sección "Mi Pasantía Activa" nueva. **Graduación Admin**: no existía ningún endpoint de listado — todo requería `alumno_id`/`proceso_id` uno por uno. Nuevo `GET /graduacion/candidatos` (paginado, admin-only, recalcula `verificar_condicion_egreso` por fila — documentado como no apto para cohortes grandes sin reescribir a SQL agregado). También se agregó `GET /graduacion/procesos/{id}/etapas`, que el frontend (`graduacionService.ts::getEtapasProceso`) ya llamaba sin que existiera (404 silencioso preexistente). `GraduacionAdmin.tsx` reescrito: 3 KPIs + `TablaPaginada` (mismo patrón que `Usuarios.tsx`) + modal de confirmación; "Verificar por ID" se mantiene como flujo secundario sin tocar su lógica. Tesina/TFG se muestra "No iniciada" cuando no hay `ProcesoGraduacion` — no se inventó tracking de tesis fuera de ese flujo. Backend: 271/271 tests ✅ (16 nuevos), migración aplicada en `neondb` y `neondb_test` (mismo drift stamp-sin-upgrade que Fase 10A, reparado igual). Frontend `tsc --noEmit`: 0 errores. Verificado en browser real (alumno: Malla + Pasantías; admin: Graduación con modal). Paso 4 (polish) del brief original queda para una sesión aparte. |
| Fase 11A — Auditoría pre-producción: índices FK | COMPLETA | 2026-07-18 | 2026-07-18 | Auditoría completa (react-doctor, tsc, pytest, endpoint coverage front/back/mobile, drift DB, N+1/paginación/índices) entregada como documento de 4 secciones antes de tocar código. Este bloque (A) corrige el hallazgo de índices: 8 columnas FK usadas en filtros/joins frecuentes sin índice — `users.role`, `asistencias.user_id`/`oferta_materia_id`, `puntajes.user_id`/`oferta_materia_id`, `inscripciones.alumno_id`/`oferta_materia_id`, `avance_alumno_pensum.alumno_id`. Ninguna tenía `index=True` en el modelo ni `create_index` en migración previa (confirmado, no era una suposición). Migración `e2f3g4h5i6j7` (`op.create_index` × 8, `downgrade` simétrico), aplicada en `neondb` y `neondb_test2`. Verificado con `inspect().get_indexes()` real en ambas DBs (no solo el stamp) — los 8 índices existen. Re-corrido el script de comparación `Base.metadata` vs `inspect()` de la Fase 3 de la auditoría: cero drift. `pytest`: 271/271 ✅. Sin cambios de código de aplicación — solo modelos (`index=True`) + migración. |
| Fase 11B — Auditoría pre-producción: eliminar N+1 | COMPLETA | 2026-07-18 | 2026-07-18 | Eliminadas las consultas N+1 en 7 endpoints. **`avance_alumno`** (pensum_router): de ~250-600+ queries a 7 queries fijas independientemente del tamaño de malla — pre-fetch batch de Materias/Correlatividades/OfertaMateria/Inscripciones/Puntajes/AvanceAlumnoPensum/prereq_nombres; nuevas funciones puras `_calcular_estado_cached` y `_tiene_nota_aprobatoria_cached` en `services/pensum.py`. **`creditos_alumno`**: de 2 queries × N a 2 queries fijas. **`obtener_malla_carrera`**: de 1 query × N filas a 1 batch. **`alumnos_asistencia`** (asistencias_router): 2 `.count()` × alumno reemplazados por 1 GROUP BY. **`profesor_alumnos`**: 1 query × alumno reemplazada por 1 batch con `IN`. **`cargar_asistencia_lote`**: 2 queries × registro reemplazadas por 2 batches previos al loop. **`listar_candidatos`** (graduacion.py): Carrera/ProcesoGraduacion/EtapaTesis batch pre-fetch (reduce de ~8 a ~5 queries/alumno; `verificar_condicion_egreso` interno no se refactorizó — documentado como deuda técnica). Funciones `_tiene_nota_aprobatoria` y `promedio_y_estado_intento` originales intactas (siguen siendo usadas por `inscripciones_router.py`). pytest: 271/271 ✅. |
| Fase 11C — Auditoría pre-producción: bugs de conexión | COMPLETA | 2026-07-18 | 2026-07-18 | Investigado con Explore agent antes de tocar código (evitó suposiciones — 4 de los 5 archivos del antipatrón `sessionStorage.getItem('token')` ya estaban limpios de sesiones previas). **1** `BecasAlumno.tsx:17` seguía con `sessionStorage.getItem('token')` (siempre `null` → `userId` siempre `0` → `getBecasActivas(0)` nunca traía datos reales); corregido a `getCurrentUser()`. **2** Foto de perfil — dos bugs reales, no uno: (a) `users_router.py::upload_foto_perfil` espera form-field `foto`, `Perfil.tsx` (2 ocurrencias, alumno+profesor) mandaba `file` → 422 silencioso en cada intento de subida; (b) la respuesta del upload es `{storage_key, url}` (URL firmada), pero el frontend leía `data.foto_url` (inexistente) → avatar nunca se actualizaba tras subir. Corregido ambos lados. Bug adicional encontrado en el mismo flujo: `GET /users/me`, `/alumno/mi-perfil` y `/alumno/dashboard` devolvían `foto_url` como **storage key cruda** (ej. `foto_perfil/uuid.jpg`), no una URL válida para `&lt;img src&gt;`/mobile `uri` — el bucket R2 requiere URL firmada. Corregido: los 3 endpoints ahora llaman `obtener_url_firmada()` antes de devolver `foto_url` (mutación en memoria del objeto ORM, sin `commit()` — no persiste la URL firmada en DB). **3** `Inscripciones.tsx::AlumnoView` llamaba `GET /materias/` sin `carrera_id`, exponiendo a cualquier alumno TODAS las materias de TODAS las carreras para preinscribirse (el backend ya soportaba el filtro `?carrera_id=`, nunca se usó). Corregido: fetch previo a `/users/me` para obtener `carrera_id` del alumno logueado, luego `GET /materias/?carrera_id={id}`. Verificado en browser real con red: `GET /materias/?carrera_id=1` confirmado. **4** `POST /auth/registro` — la pantalla "Registrarme" del login (documento+matrícula) llamaba un endpoint que nunca existió en el backend (404 en cada intento). Implementado reusando el mismo patrón que `/auth/recuperar-contrasena`: matchea `cedula`+`username` contra un usuario ya creado por un admin y envía contraseña temporal por email (no crea cuentas nuevas — activación de cuenta pre-existente). 2 tests nuevos (`test_registro_activa_cuenta_con_documento_y_matricula`, `test_registro_con_datos_incorrectos_da_404`). **Mobile**: revisado `useNotifications.ts` (`GET /notificaciones` no existe en backend, pero ya está tolerado con try/catch silencioso — no rompe la UI, documentado como gap conocido, fuera de este bloque) y `asistenciaService.ts`/`scanner.tsx` (`/asistencias/qr/verificar`, `/alumno/mis-materias`, `/alumno/mi-asistencia` — todos existen y coinciden). `UserAvatar.tsx` mobile no requirió cambios (solo consume la prop `fotoUrl`, se beneficia automáticamente del fix de URL firmada). pytest: 273/273 ✅ (271 + 2 nuevos). `npx tsc --noEmit`: 0 errores. Verificado en browser real logueado como alumno (`12345678`/`Alumno1234!`): Inscripciones con filtro de carrera confirmado por red, Becas con `userId` real (antes 0) confirmado por red, Perfil carga sin errores. Bloque D (paginación, XSS Reportes.tsx, listener leak Layout.tsx, deps de useEffect, dead clicks, mock data, bugs mobile) queda pendiente de confirmación del usuario. |
| Fase 11D — Auditoría pre-producción: hardening final | COMPLETA | 2026-07-18 | 2026-07-18 | Investigado con Explore agent antes de tocar código. **1** XSS en `Reportes.tsx:349` — `buildReportePdfHtml()` interpolaba `c.carrera` (string admin-controlado, viene de la API) sin escapar dentro de un `&lt;td&gt;` que luego se asigna vía `container.innerHTML`; agregado helper `escapeHtml()` aplicado a esa interpolación. **2** Listener leak en `Layout.tsx:124-127` — el cleanup de un `useEffect` pasaba una *función anónima nueva* a `removeEventListener('uca:help', ...)`, que nunca coincide por referencia con la registrada en `addEventListener` → el listener nunca se removía realmente; corregido guardando la función en una variable nombrada (mismo patrón ya usado correctamente en los otros 2 `useEffect` del mismo componente). **3** `react-hooks/exhaustive-deps` — cero pendientes (verificado con `eslint . --quiet`); el score bajo de la auditoría original correspondía a otras reglas, no a esta. **4** Dead clicks en `Usuarios.tsx` — dos botones sin `onClick` real ("Bloqueo Masivo", "Ver Reporte Completo") que no tenían ningún soporte de backend (no existe columna `bloqueado`/`activo` en `User`, ni endpoint de reporte de auditoría); en vez de fabricar una feature a medio hacer (prohibido por CLAUDE.md), se eliminó la UI de selección múltiple completa (`checks`, `toggleCheck`, props `selectable`/`selectedIds`/`onToggleSelect` en `TablaPaginada`) que solo existía para mostrar ese botón muerto, y el botón "Ver Reporte Completo" sin destino. **5** Mock data — revisado: los `MOCK_RESUMEN`/`MOCK_CARRERAS` de `Reportes.tsx` y `DUMMY_MATERIAS` de `mobile/app/cursos/[id].tsx` son fallbacks defensivos ante fallo de fetch (no placeholders permanentes que reemplacen datos reales) — dejados como están, documentados como deuda técnica menor (no avisan al usuario que están viendo datos de fallback). **6** Bugs mobile de react-doctor confirmados y corregidos: `mobile/app/cursos/[id].tsx:115` tenía un hook (`useTheme()`) llamado condicionalmente dentro de un `catch`+`if` dentro de un `useCallback` (doblemente inválido) y el resultado ni se usaba (`colors` ya existía en el scope del componente) — eliminado; `mobile/app/(tabs)/_layout.tsx:43` mutaba un ref (`activeTabRef.current = activeTab`) directo en el cuerpo de render — movido a `useEffect([activeTab])`; `frontend/src/pages/Asistencia.tsx:404` tenía prop `style` duplicada en el mismo `&lt;div&gt;` (React descarta la primera silenciosamente, perdiendo `marginBottom: 28`) — fusionadas en un solo objeto. **7** Paginación real agregada en 3 endpoints de listado que traían todo a memoria sin `skip`/`limit`: `GET /carreras/`, `GET /eventos/` (no aplicado a `GET /eventos/mes/{anio}/{mes}`, que ya está acotado por rango de fechas y no lo necesita), `GET /apuntes/` — mismo patrón que `materia_router.py::list_materias` (`skip=0`, `limit` default 500 máx 2000), compatibilidad hacia atrás total (siguen devolviendo array plano, no `{items,total}`). Verificado: `npx tsc --noEmit` 0 errores (frontend y mobile), `eslint . --quiet` mismos 8 errores preexistentes sin relación a este bloque (no introducidos ni resueltos), `pytest` 273/273 ✅ sin regresiones. Con este bloque se cierra la auditoría de Fase 11 completa (A/B/C/D). |
| Fase 12 — Verificación exhaustiva de errores (diagnóstico, sin fixes) | COMPLETA (diagnóstico) | 2026-07-19 | 2026-07-19 | **Solo diagnóstico, nada corregido — pendiente de confirmación para pasar a fixes.** Metodología: Fase 1 análisis estático completo (no solo archivos tocados) + Fase 2 runtime real (server real levantado, barrido de 89 endpoints GET × 3 roles = 267 requests vía script Python con tokens JWT reales; browser real navegando 44 rutas/tabs entre los 3 roles con chequeo de consola en carga Y en desmontaje/navegación-afuera) + Fase 3 cruce estático↔runtime. Detalle completo abajo. **RESUMEN EJECUTIVO: 3 bugs reales confirmados en runtime (1 alta, 2 medias), 1 gap de cobertura mobile, resto (ruff/mypy/eslint) es ruido de linter sin manifestación en runtime, confirmado por barrido de 267 requests con cero 500s.** |

| Fase 13 — Bloque E: Fix 3 bugs reales | COMPLETA | 2026-07-18 | 2026-07-18 | 3 bugs corregidos y verificados en vivo. Bug 1: `pasantias_router.py:184` — firma `subir_archivo(file_bytes,filename,prefix)` vs `subir_archivo(archivo,carpeta=...)` (TypeError → 500 cada upload). Bug 2: `Boleta.tsx:65,78` — `GET /users/` es admin-only, profesor veía dropdown vacío sin error en consola. Bug 3: `DELETE /users/{id}` no limpiaba `refresh_tokens` → 500 en refresh post-borrado. Verificado: tsc 0 errores ✅, pytest 273/273 ✅, live verification (token cleanup: 1→0, refresh da 401 no 500) ✅. Se agregó endpoint `GET /profesor/lista-alumnos` (accesible admin+profesor). Se agregó error handling visible con `console.error`+`emitToast`. |
| Fase 14 — Bloque F: Gap mobile (peer-deps + lottie + expo walkthrough) | COMPLETA | 2026-07-19 | 2026-07-19 | **1** Conflicto react@19.2.3 (pineado) vs react-dom@19.2.7 (transitiva via @expo/ui/expo-router/Radix UI): alineado subiendo react→19.2.7 (misma minor, parche superior que ya resolvió el ecosistema). **2** `@lottiefiles/dotlottie-react` agregado (peer-optional de `lottie-react-native` para web), splash animado es feature activa. Verificado: `npm install` ✅, `npx tsc --noEmit` 0 errores ✅, `npx expo export --platform web` 1487 módulos sin error ✅ (antes fallaba con `Unable to resolve "@lottiefiles/dotlottie-react"`). Tests mobile: 2/10 pasan, 8 fallos preexistentes (falta de providers en test infra, no relacionados). |
| Fase 15 — Fixes auditoría julio 2026 (Dev A + Dev C + bugs runtime) | COMPLETA | 2026-07-20 | 2026-07-20 | Cherry-pick commit `8ad6a2b` con fixes bloqueantes de auditoría: **Dev A** — Merge Alembic heads, fix migración peligrosa `b890f76d76ae`, rename `temarios→programas`, `postgresql.JSON()→sa.JSON()`, `Pasantia.motivo_rechazo`, migraciones condicionales SQLite. **Dev C** — `window.__auth_token__` eliminado, helpers `api.upload()`/`api.download()` en `lib/api.ts`, `finanzasService`/`pasantiasService`/`tramitesService` migrados, `GET /carreras/` con `Depends(get_current_user)`, router-to-router imports eliminados (`puntajes_utils.py`, `pensum.py`). **Bugfixes runtime post-cherry-pick:** corregida firma de `_to_out()` en `inscripciones_router.py` (TypeError en inscribir), agregada relación `carrera = relationship("Carrera")` en `models/materia.py` (AttributeError en profesor histórico). `pytest`: 273/273 ✅. **Queda pendiente Fase 2 del plan (`PLAN_FIXES_AUDITORIA.md`)** — mejoras backend (N+1, double-query, schemas) y frontend (Boleta fetch, hardcoded URLs, centralizar descargas). |
| Fase 16 — Mypy 0 errores + Pagos Online real + Notificaciones Push reales | COMPLETA | 2026-07-22 | 2026-07-22 | Cherry-pick commits `08564f6` + `10022fc` (7 fixes backend/frontend verificados). Auditoría integral: sessionStorage token (falso positivo), integridad referencial (30+ FKs con ondelete CASCADE/SET NULL + migración `b42cc57fda33`), CI/CD (GitHub Actions creado), seed scripts corregidos, CSRF (adecuado). Pyrefly errors corregidos. **Mypy 53→0 errores** en 15 archivos (routers/services: bool\|None→bool, int\|None→int, tipado dict, NameEmail, datetime, etc.). **Pagos Online**: stub `gateway.stub` reemplazado por Stripe Checkout Sessions reales + webhook (`POST /finanzas/pagos/webhook`) con verificación de firma. Migración `61880be1d112` (stripe_session_id en PagoOnline). **Notificaciones Push**: stub reemplazado por pywebpush real + VAPID keys auto-generadas + service worker `sw.js` + UI de suscripción en Perfil.tsx. Tests: 19/19 frontend ✅, 0 mypy errors ✅. |
| Fase 16C — Fix CSRF refresh + Pasantías Admin refinado | COMPLETA | 2026-07-22 | 2026-07-22 | **Fix CSRF en refresh**: token CSRF se perdía al refrescar página → `_readCsrfFromCookie()` recupera de cookie no-httpOnly. Sesión persiste tras F5. **Pasantías Admin**: schema `PasantiaOut` expone `created_at` + `motivo_rechazo` (existía en modelo pero no en API). Modelo `Pasantia` + migración `c0e2c42a4b9d` agrega `created_at`. Nuevo endpoint `GET /pasantias/profesores` para dropdown de tutores. Modal de aprobación reemplaza input manual por `<select>` con profesores reales. Columna "Fecha solicitud" ahora muestra `created_at` real. Tests: 19/19 frontend ✅, 15/15 backend pasantías ✅. |
| Fase 16D — Equivalencias Admin refinado | COMPLETA | 2026-07-22 | 2026-07-22 | **Equivalencias Admin**: modelo `SolicitudEquivalencia` + migración `a22743f21549` agrega `created_at`. Schema expone `created_at` + `motivo`. Nuevos endpoints `GET /equivalencias/solicitudes` (admin, todas con filtro `?estado=`) y `GET /equivalencias/materias` (dropdown). Frontend `EquivalenciasAdmin.tsx` reescrito: carga todas las solicitudes sin búsqueda manual, columna "Fecha" con `created_at` real, modales con `<select>` de materias reales. Tests: 19/19 frontend ✅, 7/7 backend equivalencias ✅. |
| Fase 17 — Ajustes Globales (Admin) con auditoría | COMPLETA | 2026-07-22 | 2026-07-22 | Módulo completo de configuración global. Modelos `GlobalSetting` + `SettingAuditLog`, migración `c8f8d13b8612`, auto-seed de 20 settings en 4 categorías (Académico/Financiero/Sistema/Notificaciones). 6 endpoints admin-only (CRUD + audit + export/import). Frontend `AjustesGlobales.tsx` con 5 tabs, edición inline por tipo, polling 30s, auditoría, export/import JSON. Ruta `/ajustes-globales` reemplaza al viejo link a `/perfil`. Tests: 8/8 settings ✅, build frontend 0 errores. |
| Fase 18 — Refinamiento visual rol profesor + Biblioteca Digital + Ajustes + Help | COMPLETA | 2026-07-22 | 2026-07-22 | **Calendario.tsx**: PDF upload admin-only, modal profesor con materia/hora/ubicación, tipos `parcial_1`/`parcial_2`. **Asistencia.tsx ProfesorView**: chips compactos, toolbar QR+timer, edición batch global, modal motivo en ausente, opción DELETE. **MisMaterias.tsx**: Activas con grade distribution bars + polling 30s + stats reales de `/puntajes/materia/{id}/estadisticas`; Histórico con year filter + trend indicators; Agenda con indicador "en vivo" + colores por tipo de evento + RecordatorioModal. **Puntajes.tsx ProfesorView**: batch save global, dirty tracking vs snapshot original, timestamp, columna Guardar eliminada, export CSV con promedio+estado, Promedio General flotante eliminado. **Estadisticas.tsx**: rewrite completo — discrimina admin (GET /reportes/dashboard) vs profesor (GET /profesor/dashboard + /puntajes/materia/{id}/estadisticas), gráficos Bar/Pie/Line, alertas de inasistencia, polling 30s. **Boleta**: ruta restringida a `['admin','alumno']`, menú profesor sin entrada. **Programa.tsx**: botón volver atrás en ProfesorProgramaView. **BibliotecaDigital.tsx**: glassmorphism redesign hero+filters+BookCard/BookRow, FAB upload real via modal con POST /apuntes/ + /apuntes/{id}/archivo, validación de contenido previa vía POST /apuntes/validar (palabras bloqueadas, relevancia contra materia, tipo de archivo, video review), FAB visible para todos los roles. **PerfilProfesor**: polling 30s con live-dot + timestamp, barras de aprobación por cátedra (verde/ámbar/rojo), tab Seguridad funcional con cambio de contraseña. **Centro de Ayuda**: botón agregado en topbar para todos los roles, modal rediseñado con 4 acciones rápidas (WhatsApp/Correo/Mi Perfil/Reportar problema) + FAQ expandible con 4 preguntas. **Logout**: confirmación modal al cerrar sesión (¿Estás seguro? / Cancelar). TipoScript + ESLint: sin errores nuevos. |
| Fase 19 — Motor de notas por puntos + Cursos unificado + auditoría real-data/mobile rol alumno | COMPLETA (código) — PENDIENTE de commit/push/deploy | 2026-07-23 | 2026-07-23 | **19A Motor de notas 0-100 (breaking, decidido explícitamente por el usuario sobre la alternativa cosmética):** nueva tabla `pesos_evaluacion` (migración `caf9713210dd`, con migración de datos: `tipo='final'→'final1'`, reescala `valor` histórico ×2 parciales/×5 final) — pesos configurables por materia (default 20/20/10/50 = 100 pts), profesor los edita en `Puntajes.tsx`. `tipo` admite `final1/final2/final3` (mejor nota de las 3 oportunidades). `calcular_promedio_final()` centralizado en `puntajes_utils.py` reemplaza ~7 reimplementaciones manuales dispersas (`boleta_router`, `alumno_router`, `reportes_router`, `pensum.py` ×4, `notas_router`) — de paso corregidos 3 bugs reales de AVG crudo mezclando escalas de puntos distintas. Diseño clave: pesos suman 100 ⇒ `promedio_0_10 = puntos/10` sigue siendo un float 0-10 válido, ningún consumidor (PPA/boleta/regularidad) rompió contrato. **19B Cursos unificado:** `Asistencia`+`Calificaciones` fusionados dentro de `Programa.tsx` (tabs Temario/Asistencia/Calificaciones) — cards circulares de asistencia con selector de período real (`GET /alumno/mis-periodos`, `GET /alumno/mis-materias?anio&semestre`), click en card abre vista de detalle full-swap (mismo patrón que Expediente Admin) con desglose completo + bitácora de sesiones. Entradas de sidebar "Asistencia"/"Calificaciones" eliminadas para rol alumno (bottom nav mobile actualizado a "Calendario" — ruta que sí sigue viva). **19C Auditoría real-data + mobile, módulo por módulo (rol alumno):** **Boleta** — período "Otoño/Primavera" fake reemplazado por año/semestre real de la inscripción (`oferta.periodo`, no currícula); desglose de notas actualizado al schema de puntos nuevo (estaba parado en el schema viejo `final` único); créditos reales vía pensum en vez de fórmula inventada; sello digital fake (QR de píxeles random + código no verificable) reemplazado por código HMAC-SHA256 real firmado con `SECRET_KEY` + QR real escaneable, nuevos endpoints `GET /boleta/{id}/sello` y `GET /boleta/verificar/{codigo}` (verificación pública de autenticidad). **Inscripción** — créditos por materia reales (antes constante fija ×4 sin importar la materia), código real de materia (`m.codigo`) en vez de `MAT-XXX` fabricado, cupo lleno ahora bloquea selección (antes solo visual), errores reales del backend (correlatividades/mora/solapamiento) mostrados en vez de toast genérico — verificado en vivo con rechazo real 422. **Malla/Expediente** — ya tenían backend 100% real de sesiones previas, se agregó polling 30s + botón Actualizar + banner de error visible (antes `.catch(()=>{})` silencioso). **Becas** — 2 bugs reales de spinner infinito: `firstLoad.current=false` corría síncrono antes de que resolviera la promesa async (loading nunca bajaba); rama `postulaciones.length===0 ? spinner : ...=== 0 ? vacío` con condición duplicada, la segunda rama nunca alcanzable. **Pasantías** — historial de solicitudes agregado (antes solo mostraba la última vía `.find()`, ocultando rechazadas/`motivo_rechazo`); form de nueva solicitud oculto si ya hay una activa. **Mis Cuotas** — 2 bugs reales de backend que crasheaban/mentían: `cuota.monto_a_pagar` no existe en el modelo ORM (solo en el schema de respuesta) → `AttributeError` 500 en cada intento de pago online; el mismo bug en `/alumno/dashboard` estaba enmascarado por un `except Exception: pass` silencioso, por lo que **el sistema entero mostraba "Sin adeudos"/Gs. 0 para cualquier alumno con cuotas, siempre** — bonus bug ahí mismo: comparaba `estado=='vencido'` contra el valor real `'vencida'`. Pago online sigue bloqueado por falta de clave Stripe real (`sk_test_placeholder` en `.env`, pendiente de que el usuario la genere). **Trámites** — poll+refresh agregado a vista alumno (admin ya lo tenía); mensaje "Trámites próximamente disponibles" fake se mostraba tanto si el catálogo estaba vacío de verdad como si el fetch fallaba por error real — separados. **Perfil/Ajustes** — Promedio General mezclaba puntos de escalas distintas (parcial 20pts + final 50pts) en un AVG plano sin pesos; corregido a usar `/alumno/mis-notas` real; agregado polling. **Login Admin** — colores unificados (variables `--cyan-*` mezclaban un teal viejo `rgba(0,180,216,…)` con el azul real `#2563eb`, gradiente de "control total" hardcodeado a un tono distinto); bug real de mobile: `border-bottom` en el panel decorativo quedaba como raya suelta al superponerse con el panel de formulario (`margin-top:-60px`), y ese mismo negative-margin pegaba el logo contra el texto del hero — corregido moviendo el overlap solo al `.form-content`. **Dashboard mobile** — tablas "Calificaciones por Materia" (alumno, 7 columnas) y "Cursos Activos" (profesor, 4 columnas sin wrapper de overflow) reemplazadas por cards apiladas debajo de 680px; mismo fix aplicado al Desglose de Materias de Boleta. Bottom-nav QR ahora es botón circular flotante elevado (estilo app mobile) en vez de ícono plano en la fila. **Pendiente explícito, con decisión del usuario:** pago online no probado end-to-end (falta clave Stripe real); rediseño visual tipo card+hero del login alumno/profesor (template pegado por el usuario) pausado a pedido explícito, sin tocar. Tests: backend 273/273 ✅ (sin regresiones sobre el motor de notas nuevo), frontend build 919 módulos 0 errores. **114 archivos modificados/nuevos sin commitear, rama `push-final` 47 commits adelante de `origin/sistema-academico-v2` sin push — ver checklist de pre-producción abajo antes de desplegar.** |

## Fase 12 — Detalle del diagnóstico (2026-07-19)

### Bugs reales confirmados (prioridad para próxima sesión de fixes)

| Archivo:línea | Tipo | Severidad | Momento | Descripción |
|---|---|---|---|---|
| `backend/app/routers/pasantias_router.py:184` | **Runtime (confirmado en vivo)** | 🔴 Alta | Al invocar la acción (no en carga) | `subir_archivo(archivo, carpeta="informes_pasantia")` — la firma real es `subir_archivo(file_bytes, filename, prefix)`. `POST /pasantias/{id}/informes` tira `TypeError: subir_archivo() got an unexpected keyword argument 'carpeta'` → 500, **cada vez** que un alumno/profesor/admin adjunta un archivo a un informe de pasantía. Reproducido en vivo con curl + archivo real, traceback capturado. Detectado primero por mypy (`call-arg`), confirmado por request real. |
| `frontend/src/pages/Boleta.tsx:65,78` | **Runtime (confirmado en vivo, red)** | 🔴 Alta | Al cargar la página (rol profesor) | `GET /users/` es admin-only (`users_router.py::list_users` chequea `role != "admin"` → 403). `Boleta.tsx` lo llama también para la vista de **profesor** (rama `!esAlumno`) para poblar el selector de alumno. Confirmado con browser real + login profesor real: `GET /users/ → 403 Forbidden` en Network, dropdown queda permanentemente vacío. El error se traga en `.catch(() => {})` — **no aparece en consola**, solo se detecta probando la funcionalidad real. Feature "Boleta" 100% no funcional para rol profesor. No lo detecta ningún linter/tsc (es una llamada de API válida en TS, solo falla por rol en runtime). |
| `backend/app/routers/auth_router.py:207,211` + `backend/app/routers/users_router.py:110` | **Estático confirmado, runtime no reproducido en vivo (destructivo)** | 🟡 Media | Solo si el usuario fue borrado mientras el JWT/refresh token sigue vivo | `DELETE /users/{user_id}` (`users_router.py:159-188`) borra asistencias/puntajes/inscripciones/foro/apuntes del usuario pero **no borra `refresh_tokens`**. Si un admin borra un usuario con sesión activa, su próximo `POST /auth/refresh` hace `db.query(User)...first()` → `None`, y accede a `db_user.username`/`.role`/`.id` sin chequeo → `AttributeError` → 500 en vez de 401 limpio. Mismo patrón en `users_router.py:110` (`upload_foto_perfil`) sin `if not user: raise 404`. No reproducido en vivo para no destruir datos de seed — confirmado por lectura de código + mypy (`union-attr` en las 3 ubicaciones). |

### Gaps de cobertura (no son bugs de código, son límites de esta pasada de verificación)

- **Mobile Fase 2 no se pudo completar.** `expo start --web` fallúa el bundling: `Unable to resolve "@lottiefiles/dotlottie-react"` desde `lottie-react-native/lib/module/LottieView/index.web.js`, importado por `SplashAnimated.tsx` → `app/_layout.tsx`. `@lottiefiles/dotlottie-react` no está en `package.json` como dependencia. Bloquea cualquier verificación de consola vía navegador para mobile en este entorno (sin emulador/dispositivo físico disponible). Recomendado: agregar la dependencia si se quiere soportar `expo start --web`, o verificar manualmente en Expo Go/simulador antes de la próxima sesión.
- **Mobile no tiene ESLint configurado** (`package.json` sin script `lint`, sin config). Al intentar instalarlo automáticamente (`npx expo lint`), reveló un conflicto real de peer-dependencies: `react@19.2.3` (pineado en el proyecto) vs `react-dom@19.2.7`/`@expo/ui`/`expo-router` (radix-ui) que piden `react@^19.2.7`. No causó fallo visible todavía, pero es terreno fértil para un bug futuro difícil de diagnosticar. No se instaló nada (`--legacy-peer-deps` no se corrió, por ser diagnóstico only).

### Estático — ruff (backend), 31 hallazgos, todos ruido de linter salvo 1 nota de código muerto

Ninguno de estos 31 causa un bug en runtime (confirmado por el barrido de 267 requests sin 500s). Son imports sin usar, comparaciones `== True`, orden de imports, variables sin usar — cosmético/mantenibilidad.
- **Nota real de código muerto**: `backend/app/routers/pensum_router.py:8-9` — `_tiene_nota_aprobatoria` y `_tiene_inscripcion` importados pero nunca usados, remanente del refactor de N+1 de la Fase 11B (Bloque B) que reemplazó las llamadas por las versiones `_cached`. Sin impacto — cosmético, pero vale la pena limpiar en la próxima pasada.
- Detalle completo (archivo:línea) en el log de la sesión — 31 líneas, disponible on-demand si se quiere la lista exacta antes de fixear.

### Estático — mypy (backend), 290 hallazgos: ~266 ruido sistémico ya documentado + 24 revisados individualmente

`~266/290` son el patrón `Column[T]` vs `T` ya documentado en Fase 6B (el codebase usa `Column()` clásico de SQLAlchemy 1.x, no `Mapped[]` de 2.0 — mypy no entiende que a runtime `Column[int]` se resuelve a `int`). **No son bugs reales**, confirmado otra vez por el barrido de 267 requests sin 500s.

De los 24 hallazgos con reglas distintas (`call-overload`, `union-attr`, `index`, `list-item`, `return-value`, `misc`, `str`, `call-arg`), revisados uno por uno:
- **3 son bugs reales** (ver tabla de arriba: `pasantias_router.py:184`, `auth_router.py:207/211`, `users_router.py:110`).
- El resto (`app/services/pensum.py:91`, `app/services/equivalencia.py:87`, `app/services/tramites.py:263`, `app/email_utils.py:76,105`, `app/services/facturacion_electronica.py:85`, `app/routers/puntajes_router.py:23,327`, `app/routers/notas_router.py:118,153,156`, `app/routers/asistencias_router.py:54,335,338`, `app/routers/alumno_router.py:185`, `app/routers/reportes_router.py:252`) son el mismo ruido `Column[T]`/`dict.get()` overload confuso — no manifestaron ningún 500 en el barrido completo de endpoints. Ruido, no bugs.

### Estático — eslint (frontend), 8 errores + 6 warnings, ninguno con impacto runtime confirmado

- `Asistencia.tsx:3` — imports `decodeToken`/`getAccessToken` sin usar. Código muerto real del refactor de Fase 10A (se reemplazaron por `getCurrentUser()` pero no se limpiaron los imports). Cero impacto runtime (nunca se llaman).
- `BecasAlumno.tsx:151`, `ExpedienteAdmin.tsx:78`, `GraduacionAdmin.tsx:117`, `PasantiasAdmin.tsx:131`, `SolicitudesTramites.tsx:65` — regla `react-hooks/set-state-in-effect` (setState síncrono dentro de un efecto). Advertencia de patrón, no bug de comportamiento — confirmado sin problema visible en las 44 visitas de rutas del walkthrough manual.
- 6 warnings `exhaustive-deps` (`Asistencia.tsx:43`, `EquivalenciasAlumno.tsx:30`, `Foro.tsx:43`, `MallaAdmin.tsx:38`, `MisMaterias.tsx:103,128`) — deps potencialmente stale, pero sin manifestación observada en runtime durante el walkthrough (los valores omitidos son estables en el patrón de uso actual de la app).
- `Layout.test.tsx:8` — `prefer-const`, nit de archivo de test, cero impacto de producción.

### Estático — tsc, 0 errores en frontend y mobile (`--noEmit` completo, no solo archivos tocados)

### Runtime — backend, 89 endpoints GET × 3 roles (267 requests), cero 500 salvo el confirmado arriba

Todos los no-200 fueron 404 (ID de prueba `1` no existe — falta de dato, no bug), 422 (falta query param requerido, esperado sin pasarlo), o 403 (restricción de rol funcionando como se diseñó). Servidor real levantado (`uvicorn`), tokens JWT reales de las 3 cuentas seed (`admin@uca.edu.py`, `12345678`, `prof@uca.edu.py`).

### Runtime — frontend, 44 rutas/tabs visitadas en browser real, cero errores de consola (carga y salida)

Alumno (18 rutas), Profesor (9 rutas), Admin (17 rutas + 6 sub-tabs de Finanzas) — incluye login, logout (unmount completo del Layout), y navegación entre rutas (unmount de cada página anterior). Cero errores rojos en consola en ningún punto, incluida la limpieza de listeners de `Layout.tsx` corregida en Fase 11D (verificado indirectamente: si el fix no hubiera funcionado, navegar Ayuda→otra ruta repetidamente habría acumulado listeners duplicados sin causar error de consola visible de todos modos — no es una prueba concluyente de esa regla puntual, pero confirma que no hay error visible).

Estados válidos: PENDIENTE / EN_PROGRESO / EN_REVISION / COMPLETA

---

## Notas pendientes para próxima sesión

### Deuda técnica menor (no bloquea Fase 1)

- **Tests postgres_compat necesitan DB de test separada en Neon** — RESUELTA (2026-07-06).
  Branch `neondb_test` creada en el mismo proyecto Neon. `TEST_DATABASE_URL` vive en
  `backend/.env.test` (gitignored, nunca commiteado — verificado con
  `git log --all --full-history`). `tests/conftest.py` carga ese archivo con
  `load_dotenv` antes de importar `app.main`. Suite completa: 79 passed, incluyendo
  los 4 tests de `test_postgres_compat.py` (antes skipped) corriendo contra
  `neondb_test`. Confirmado por conteo de filas antes/después que `neondb`
  (desarrollo) no fue tocada.

### Infra cerrada durante diagnóstico de Fase 1 (2026-07-06)

- **Migración Materia → Materia + OfertaMateria** — RESUELTA. `Materia.profesor_id`
  fijo no permitía distinguir profesor por período (bloqueante real para 1.4.3
  Histórico). Nueva tabla `ofertas_materia` (materia_id + profesor_id + periodo +
  activa). `Inscripcion`/`Puntaje`/`Asistencia` migrados a `oferta_materia_id`
  (arregla también colisión de `UniqueConstraint` para alumnos repitentes).
  21 puntos de autorización consolidados en `app/services/autorizacion.py`.
  Migración `n1o2p3q4r5s6` aplicada en `neondb_test` (validada con dato sembrado)
  y en `neondb` (dev real, con backup lógico previo en
  `backend/backup_neondb_pre_migracion_2026-07-06.json`, gitignored).
- **Drift `uq_materia_nombre_carrera` vs `materias_nombre_key`** — RESUELTA.
  El schema real de `neondb` tenía la tabla `materias` creada por
  `Base.metadata.create_all()` en Fase 0, nunca por una revisión Alembic —
  su UNIQUE constraint real (`materias_nombre_key`, solo sobre `nombre`) no
  coincidía con el modelo (`nombre`+`carrera_id`). Migración `o2p3q4r5s6t7`
  corrige el schema real para que coincida con el modelo. Sin duplicados
  verificados antes de aplicar.
- **Hallazgo colateral:** se encontró y eliminó una tabla `ofertas_materia`
  huérfana en `neondb` (creada manualmente fuera del flujo de Alembic,
  confirmado con el usuario, vacía, sin riesgo) antes de aplicar la migración
  real — `alembic_version` nunca reflejó ese cambio manual.
- **Nota de infra:** `neondb_test` quedó inaccesible (`password authentication
  failed`) durante el cierre de esta tarea — problema de la branch en Neon,
  no relacionado con el código. La migración del drift se validó por inspección
  directa (chequeo de duplicados) y se aplicó solo en `neondb` con el visto
  bueno explícito del usuario. Revisar la branch de test antes de la próxima
  sesión.

### Fase 1 — cierre (2026-07-06/07)

- **1.1** — `estadisticasService.ts` nuevo, `Estadisticas.tsx` sin mocks, consume
  `/puntajes/materia/{id}/estadisticas` por materia (N llamadas agregadas
  client-side, ya que el endpoint es por-materia y el dashboard es institucional).
  Asistencia/alertas siguen en fetch crudo (ya eran reales, solo se sacó el
  fallback a mock). Estado vacío explícito cuando `total_notas=0`.
- **1.2** — `PATCH /foro/mensajes/{id}` (ventana 15 min), `GET /foro/hilos/{id}/mensajes`
  paginado (reemplaza mensajes embebidos), botones Fijar/Cerrar conectados al
  `PUT` existente (que ahora sí valida titularidad del profesor — antes cualquier
  profesor podía fijar/cerrar hilo ajeno). Bug de auth (`sessionStorage` nunca
  seteado) corregido en `Foro.tsx`.
- **1.3** — `GET /users/` acepta `skip/limit/q/role`, devuelve `{items,total}`.
  `TablaPaginada.tsx` nuevo en `src/components/common/` (genérico, controlado,
  reutilizable en Fase 4). `Usuarios.tsx` sin fetch-todo-y-cortar.
- **1.4** — `OfertaMateria` (ya resuelto antes) + `recordatorios_docente` (Alembic
  `p3q4r5s6t7u8`) + `GET /profesor/mi-historico` + `GET /profesor/mi-agenda` +
  CRUD `/profesor/recordatorios`. `MisCursos.tsx` eliminado, `MisMaterias.tsx`
  reescrito con 3 pestañas (Activas/Histórico/Agenda). Rutas `/miscursos` y
  `/mismaterias` redirigen a `/mis-materias`; menú desktop y bottomnav mobile
  colapsados a 1 entrada.
- **Bug transversal encontrado y corregido 3 veces esta fase:** `Foro.tsx`,
  `MisCursos.tsx`/`MisMaterias.tsx` y `hooks/useRole.ts` (este último usado por
  `Layout.tsx` para *todo* el menú/bottomnav de *todos* los roles) leían
  `sessionStorage.getItem('token')`, que nunca se setea — el token vive en
  memoria (`lib/api.ts`). Efecto real: el menú lateral y bottomnav mostraban
  **siempre** la navegación de alumno, sin importar el rol logueado. Corregido
  a `getCurrentUser()` en los tres lugares.
- **Nota de infra:** `neondb_test` siguió inaccesible (`password authentication
  failed`) en esta sesión — la migración de `recordatorios_docente` (aditiva,
  bajo riesgo) se aplicó directo en `neondb`. Revisar la branch de test antes
  de la próxima sesión de Fase 2.

### Resuelto — compute de `neondb_test` recreado (2026-07-08)

Tras 3 sesiones consecutivas con el error `endpoint could not be found` (compute
suspendido por inactividad en free tier de Neon), se creó una **nueva branch** en
Neon (`neondb_test2`, basada en `main`) con **auto-suspensión desactivada (Never)**
para evitar que vuelva a caerse. `TEST_DATABASE_URL` actualizada en `.env.test`.

Migrations aplicadas contra la nueva branch:
```powershell
$env:DATABASE_URL = (Get-Content .env.test) -replace "TEST_DATABASE_URL=", ""
alembic upgrade head
```

`alembic current` confirma `r5s6t7u8v9w0 (head)` en `neondb_test2`. Tests de
compatibilidad Postgres pueden volver a correr sin restricción.

Verificación visual completada (sesión previa): login admin y alumno contra `neondb`.
`ExpedienteAdmin` — búsqueda, badge "ACTIVO", estado vacío correcto.
`ExpedienteAlumno` — PPA "—", "0 créd. computados", badge "ACTIVO", estado vacío
correcto. Menú "Calificaciones" y "Expediente" separados correctamente.

### Fase 7 — cierre (2026-07-11)

- **7A Frontend tests**: 19/19 ✅ en vitest. Mobile: 9/10 fallan por RN mocks, no por bugs.
- **7B Design System**: deferido permanentemente. Login pages + componentes legacy usan CSS autocontenido que no se puede reemplazar con tokens sin regresión. Costo cosmético vs riesgo de rotura no justifica.
- **7C Becas UI**: `BecasAlumno.tsx`, ruta `/mis-becas`, menú alumno. Build ✅.
- **7D Contraseña**: Perfil.tsx password change conectado a API real (PATCH según rol).
- **7E Exámenes**: modelos + router + tests 15/15. Sin `turno`/`habilitado`/`cierre_inscripcion`/`oferta_materia_id` — el modelo real usa `hora_inicio`, `tipo`, `periodo`, `cupos`, `estado`, `profesor_id`.
- **7F.1 Pagos Online**: stub de Bancard con tabla `pagos_online`, migration manual (sin acceso a Postgres local), 3 endpoints.
- **7F.2 Notificaciones Push**: stub de Web Push con `SuscripcionPush` y router `notificaciones_router`.
- **Migración `c6d7e8f9g0h1`**: creada manualmente (no autogenerated — Postgres no disponible localmente). Debe aplicarse en `neondb`/`neondb_test` con `alembic upgrade head`.
- **Suite backend**: 80 tests clave ✅ — sin regresiones.

---

## Checklist de pre-producción (2026-07-23, tras Fase 19)

No es una fase de código — es lo que falta **fuera del código** antes de poner el sistema en un dominio real. Verificado contra el estado real del repo (`git status`, `alembic heads`, `.env.example`), no supuesto.

### Bloqueante — control de versiones
- [ ] **114 archivos modificados/nuevos sin commitear** en `push-final` (incluye todo lo de Fase 19 + módulos sin trackear: `AjustesGlobales.tsx`, `settings_router.py`, `pagos_online.py`, `notificaciones_push.py`, `sw.js`, 6 migraciones nuevas). Nada de esto existe fuera de este disco hasta que se commitee.
- [ ] `push-final` está **47 commits adelante de `origin/sistema-academico-v2`** sin pushear — todo el trabajo de las últimas sesiones vive solo local.
- [ ] El repo tiene 15 branches divergentes (`main`, `develop`, `sistema-academico-v2`, `push-final`, `v2-develop`, `dev-a-local`, etc.), varias con commits que las otras no tienen. **Antes de deployar hay que decidir cuál es la rama canónica de producción** — deployar sin resolver esto arriesga perder trabajo de otra rama o pisar código más nuevo.

### Bloqueante — secretos de producción (`backend/.env`)
Todos gateados con degradación con gracia (no crashean si faltan), pero las features asociadas no funcionan sin ellos:
- [ ] `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY`/`STRIPE_WEBHOOK_SECRET` — hoy en `sk_test_placeholder`. Pago online 100% no funcional hasta cargar claves reales (ver instrucciones ya enviadas al usuario para sacarlas gratis en modo test).
- [ ] `GUARANI_APP_API_KEY`/`GUARANI_APP_PUNTO_EMISION` — vacíos. Facturación electrónica degrada a comprobante en estado `error` (reintentable, no bloquea el pago) pero no emite comprobante real.
- [ ] `R2_ENDPOINT_URL`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME` — usados por `app/services/storage.py` (fotos de perfil, apuntes, comprobantes, informes de pasantía) pero **faltan por completo en `.env.example`** — hay que confirmar que están seteados en el `.env` real de producción y agregarlos al ejemplo.
- [ ] `MAIL_USERNAME`/`MAIL_PASSWORD` (Gmail app password) — confirmar que la cuenta de envío de producción es la real, no la de dev.
- [ ] `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` — se autogeneran si están vacías, pero autogenerar en cada deploy invalida las suscripciones push existentes; generar una vez y fijarlas.
- [ ] `JWT_SECRET` — confirmar que el valor de producción es distinto del de dev y tiene ≥32 chars aleatorios reales.
- [ ] `COOKIE_SECURE=true` — ya es el default correcto para HTTPS; solo falla si el dominio de producción no tiene HTTPS (no debería pasar).
- [ ] `CORS_ORIGINS` — hoy apunta a `http://localhost:5173`. Cambiar al dominio real de producción antes de servir el frontend ahí (si no, el browser bloquea todos los fetch por CORS).

### Bloqueante — infraestructura de hosting (no existe todavía)
- [ ] No hay `Dockerfile` de la app (el `docker-compose.yml` existente solo levanta Postgres local, no sirve para deployar). No hay `render.yaml`/`vercel.json`/`netlify.toml`/`fly.toml` ni ningún config de proveedor.
- [ ] No se eligió proveedor de hosting ni dominio. Decisión pendiente del usuario: backend (Render/Railway/Fly.io — necesita proceso long-running por los jobs de reintento de facturación) + frontend (Vercel/Netlify/Cloudflare Pages, build estático `npm run build` ya verificado) + DB (ya en Neon, solo hay que confirmar branch de producción vs dev/test) + DNS del dominio real.
- [ ] `.github/workflows/ci.yml` existe pero está sin trackear (`??` en git status) — confirmar que corre y qué cubre antes de asumir que hay CI real protegiendo la rama de producción.

### Verificar antes de ir a producción (no bloqueante si se confirma OK)
- [ ] Migraciones: `alembic heads` da un solo head (`caf9713210dd`) y `alembic current` contra la Neon branch de este entorno de dev coincide — confirmar que la branch de Neon que se use en producción (si es distinta) también está en `head` antes del primer deploy.
- [ ] Semilla de datos: `seed.py`/`seed_completo.py` crean usuarios de prueba (`admin@uca.edu.py`, `12345678`, `prof@uca.edu.py`) con contraseñas conocidas — **no correr contra la DB de producción**, o rotar esas contraseñas antes de abrir el sistema a usuarios reales.
- [ ] App mobile (Expo) sigue en rama `mobile-v1-completa` con PR pendiente de apertura (Fase 7G) — fuera del alcance de "la web", pero si el plan es publicar ambas, falta ese merge + publish a stores/Expo.

### Ya verificado, no es bloqueante
- CORS/cookies/CSRF ya están parametrizados correctamente por env var (no hardcodeados) — solo falta setear los valores reales.
- `npm run build` (frontend) — 919 módulos, 0 errores.
- `pytest` (backend) — 273/273 sin regresiones.