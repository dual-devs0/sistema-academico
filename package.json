{
  "scripts": {
    "check": "cd frontend && npm run check",
    "check:backend": "cd backend && ruff check . --select E,F,W && mypy app"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "motion": "^12.42.0",
    "react-router-dom": "^7.18.0"
  }
}
